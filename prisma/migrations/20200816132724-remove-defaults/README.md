# Migration `20200816132724-remove-defaults`

This migration has been generated by Nick Reynolds at 8/16/2020, 1:27:24 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql

```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200816132251-add-auto..20200816132724-remove-defaults
--- datamodel.dml
+++ datamodel.dml
@@ -1,8 +1,8 @@
 // 1
 datasource db {
     provider = "postgresql"
-    url = "***"
+    url = "***"
 }
 // 2
 generator client {
@@ -11,41 +11,41 @@
 // 3
 model Muscle {
-    id           Int            @id @default(autoincrement())
+    id           Int            @id
     createdAt    DateTime       @default(now())
     name         String
     description  String
     MuscleImpact MuscleImpact[]
 }
 model MuscleImpact {
-    id           Int       @id @default(autoincrement())
+    id           Int       @id
     muscle       Muscle    @relation(fields: [muscleId], references: [id])
     impactRating Int
     muscleId     Int
     Exercise     Exercise? @relation(fields: [exerciseId], references: [id])
     exerciseId   Int?
 }
 model Measurable {
-    id         Int       @id @default(autoincrement())
+    id         Int       @id
     name       String
     Exercise   Exercise? @relation(fields: [exerciseId], references: [id])
     exerciseId Int?
 }
 model Format {
-    id          Int        @id @default(autoincrement())
+    id          Int        @id
     createdAt   DateTime   @default(now())
     name        String
     description String
     Exercise    Exercise[]
 }
 model Exercise {
-    id            Int            @id @default(autoincrement())
+    id            Int            @id
     createdAt     DateTime       @default(now())
     name          String
     description   String
     format        Format         @relation(fields: [formatId], references: [id])
@@ -57,9 +57,9 @@
     measurables   Measurable[]
 }
 model Set {
-    id          Int        @id @default(autoincrement())
+    id          Int        @id
     exercise    Exercise   @relation(fields: [exerciseId], references: [id])
     restSeconds Int
     exerciseId  Int
     createdBy   User       @relation(fields: [createdById], references: [id])
@@ -67,17 +67,17 @@
     createdById Int
 }
 model SetGroup {
-    id               Int               @id @default(autoincrement())
+    id               Int               @id
     sets             Set[]
     routineRevisions RoutineRevision[]
     createdBy        User              @relation(fields: [createdById], references: [id])
     createdById      Int
 }
 model RoutineRevision {
-    id          Int        @id @default(autoincrement())
+    id          Int        @id
     createdAt   DateTime   @default(now())
     routineBlob String
     setGroups   SetGroup[]
     routine     Routine?   @relation(fields: [routineId], references: [id])
@@ -86,9 +86,9 @@
     createdById Int
 }
 model Routine {
-    id          Int               @id @default(autoincrement())
+    id          Int               @id
     createdAt   DateTime          @default(now())
     name        String
     description String
     color       String
@@ -97,9 +97,9 @@
     revisions   RoutineRevision[]
 }
 model User {
-    id              Int               @id @default(autoincrement())
+    id              Int               @id
     username        String            @unique
     password        String
     routines        Routine[]
     exercises       Exercise[]
```


