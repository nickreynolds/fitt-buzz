# Migration `20201102041022-update-recording-models`

This migration has been generated by Nick Reynolds at 11/1/2020, 11:10:22 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "public"."RoutineRevisionRecording" ADD COLUMN "completedSetGroups" integer  NOT NULL ;

ALTER TABLE "public"."SetGroupRecording" ADD COLUMN "completedSets" integer  NOT NULL ,
ADD COLUMN "routineRevisionRecordingId" text   ;

ALTER TABLE "public"."SetGroupRecording" ADD FOREIGN KEY ("routineRevisionRecordingId")REFERENCES "public"."RoutineRevisionRecording"("id") ON DELETE SET NULL ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20201102001558-add-recording-models-first-pass..20201102041022-update-recording-models
--- datamodel.dml
+++ datamodel.dml
@@ -4,9 +4,9 @@
 ///
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url = "***"
 }
 model Exercise {
   createdAt         DateTime            @default(now())
@@ -55,24 +55,29 @@
   RoutineRevisionRecording RoutineRevisionRecording[]
 }
 model RoutineRevisionRecording {
-  createdAt         DateTime        @default(now())
-  createdById       String
-  id                String          @id
-  createdBy         User            @relation(fields: [createdById], references: [id])
-  revision          RoutineRevision @relation(fields: [routineRevisionId], references: [id])
-  routineRevisionId String
+  createdAt          DateTime            @default(now())
+  createdById        String
+  id                 String              @id
+  createdBy          User                @relation(fields: [createdById], references: [id])
+  revision           RoutineRevision     @relation(fields: [routineRevisionId], references: [id])
+  routineRevisionId  String
+  completedSetGroups Int
+  setGroupRecordings SetGroupRecording[]
 }
 model SetGroupRecording {
-  createdAt    DateTime       @default(now())
-  createdById  String
-  id           String         @id
-  createdBy    User           @relation(fields: [createdById], references: [id])
-  setGroup     SetGroup       @relation(fields: [setGroupId], references: [id])
-  setGroupId   String
-  SetRecording SetRecording[]
+  createdAt                  DateTime                  @default(now())
+  createdById                String
+  id                         String                    @id
+  createdBy                  User                      @relation(fields: [createdById], references: [id])
+  setGroup                   SetGroup                  @relation(fields: [setGroupId], references: [id])
+  setGroupId                 String
+  setRecordings              SetRecording[]
+  completedSets              Int
+  RoutineRevisionRecording   RoutineRevisionRecording? @relation(fields: [routineRevisionRecordingId], references: [id])
+  routineRevisionRecordingId String?
 }
 model SetRecording {
   id                  String              @id
```


